<div class="form-group-shadow"></div>
<div class="form-group form-group-button row">
    <div class="button-group">
    <div class="col-xs-3">
        <a href="#" data-action="menu:show" class="btn btn-lg btn-block btn-default">
            <i class="fa fa-cog"></i>
        </a>
    </div>
    <div class="col-xs-9">
        <button type="submit" class="btn btn-lg btn-block btn-primary">
            <i class="fa fa-lock"></i>
            <%= _('Pay') %>
            <b><span x-text="amount"><%= params.attr('amount') %></span> <%= params.attr('currency') %></b>
        </button>
    </div>
    </div>
</div>